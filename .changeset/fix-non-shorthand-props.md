---
'vite-plugin-flatten-ns': patch
---

handle non-shorthand compound object props (`{ Root: BentoGridRoot }`), add inline enrichment for Pass 2 module loading, use esbuild `jsx: 'automatic'` to avoid React-not-defined errors
